/// Mirrors the backend's `AuditStatusSeedIds` — keep values in sync with
/// IMIS.Domain.AuditStatusSeedIds.
class AuditStatusSeedIds {
  static const int draft = 1;
  static const int pending = 2;
  static const int approved = 3;
  static const int disapproved = 4;
}
